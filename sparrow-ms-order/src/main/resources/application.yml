
server:
  port: 10002
management:
  endpoints:
    web:
      base-path: /
spring:
  application:
    name: sparrow-ms-order
debug: true
#  profiles:
#    active: dev # 会被spring.cloud.config.profile指定的远程配置文件替代
