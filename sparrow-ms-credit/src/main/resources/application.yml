
server:
  port: 10003
management:
  endpoints:
    web:
      base-path: /
spring:
  application:
    name: sparrow-ms-credit
debug: true
#  profiles:
#    active: dev # 会被spring.cloud.config.profile指定的远程配置文件替代
