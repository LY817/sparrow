eureka:
  client:
    service-url:
      defaultZone: http://${cmd.server}:8761/eureka/

# zuul路由配置
zuul:
  routes:
    sparrow-ms-inventory: /inventory/**
    sparrow-ms-order: /order/**
#spring:
#  cloud:
#    config:
#      # 从注册中心发现config服务
#      discovery:
#        service-id: sparrow-config
#        enabled: true
#      # 指定对应配置文件
#      label: master
#      profile: dev