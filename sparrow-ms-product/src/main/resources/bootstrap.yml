eureka:
  client:
    service-url:
      defaultZone: http://${cmd.server}:8761/eureka/
  instance:
    hostname: localhost
    metadata-map:
      vendor: hik
#spring:
#  cloud:
#    config:
#      # 从注册中心发现config服务
#      discovery:
#        service-id: sparrow-config
#        enabled: true
#      # 指定对应配置文件
#      label: master
#      profile: dev